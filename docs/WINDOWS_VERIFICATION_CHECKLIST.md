# Windows実機検証チェックリスト(Epic β-001 統合後)

このチェックリストは、`main` に統合された Epic β-001(Phase 1〜8, PR #184)、
および `apps/desktop` のTauri移行(PR #184後の次フェーズ、
`docs/ai-handoff/CLAUDE_RESPONSE.md` 参照)を実際の Windows 端末で検証するための
手順である。サンドボックス開発環境では `libwebkit2gtk-4.1-dev` 等のシステム
ライブラリがネットワークポリシー上インストールできず `cargo build` 自体が
完了しないため、下記項目はすべて**実機での確認が未実施**である。

> **前提**: `apps/desktop` は Tauri ベースに移行済み(`docs/ARCHITECTURE.md`
> 第0.2章参照)。Electron実装は git 履歴にのみ残る。

## 0. 事前準備

- [ ] Windows 10/11 端末を用意する
- [ ] Node.js(`package.json` の `engines` または CI 設定と同じメジャーバージョン)をインストール
- [ ] Rust ツールチェイン(`rustup`)と Tauri の Windows ビルド要件
  (Microsoft Visual Studio C++ Build Tools、WebView2 ランタイム。
  <https://tauri.app/start/prerequisites/> 参照)をインストール
- [ ] リポジトリを `git clone` し、対象ブランチ(`main`)を checkout する

## 1. ビルド確認

- [ ] `npm install` がエラーなく完了する
- [ ] `npm run build` がモノレポ全体でエラーなく完了する
  (`packages/shared`, `avatar-2d`, `ai-core`, `integrations`, `voice-analysis`,
  `voice-engine` → 各 `apps/*` の順にビルドされることを確認)
- [ ] `npm run test --workspaces --if-present` で全ユニットテストが green になる
- [ ] `cd apps/desktop/src-tauri && cargo check` がエラーなく完了する
  (このサンドボックス環境では未検証。Windows実機では
  `libwebkit2gtk-4.1-dev` 相当の制約はない)

## 2. Tauriアプリ起動確認(`apps/desktop`)

- [ ] `npm --prefix apps/desktop run dev`(内部で `tauri dev` を呼ぶ)でアプリが起動する
- [ ] メインウィンドウが表示され、白画面にならない
- [ ] メインウィンドウ内に Sales Workspace の UI が正しく描画される
- [ ] コンソール/ログにアセット読み込みエラーが出ていない
- [ ] `npm --prefix apps/desktop run package:win`(`tauri build`)でWindows向け
  バンドルが生成される

## 3. システムトレイ・自動起動

- [ ] アプリ起動後、システムトレイにアイコンが常駐する
- [ ] トレイアイコンのメニュー(表示/終了など)が機能する
- [ ] ウィンドウを閉じてもアプリがトレイに残る(意図した挙動であることを確認)
- [ ] 自動起動設定を有効にした場合、OS再起動後にアプリが自動的に起動する

## 4. MUSA常駐アバター(`avatar.html` / `packages/avatar-2d`)

- [ ] アバターのオーバーレイウィンドウ(フレームレス・透過・最前面)が表示される
- [ ] Sales Workspaceの「デモ通話を分析」操作で、アバターの状態遷移が
  オーバーレイウィンドウに反映される(Tauriイベント経由の往復確認)
- [ ] アバターウィンドウ・メインウィンドウのいずれかを閉じても、
  もう一方や本体アプリがクラッシュしない

## 5. Sales Workspace UI

- [ ] リード一覧が表示される
- [ ] 日次プラン(Daily Plan)が表示され、推奨アクションが算出されている
- [ ] KPI表示が正しく計算されている(0〜100%の範囲に収まっていること)
- [ ] Settings画面が開ける

## 6. FileMaker / Zoom Phone 連携(Mockのみ、実接続はしない)

- [ ] Mockアダプタ経由でのデータ取得・表示が正しく動作する
- [ ] 設定タブの連携準備UI(接続ステータス表示、ダミー値専用の入力フォーム)が
  表示・操作できる
- [ ] **実際のFileMaker Data API / Zoom Phone APIへの接続は行わないこと**
  (本フェーズでは未対応。実接続の実装は次フェーズ以降)

## 7. Voice Engine / Voice Analysis(Mock入力)

- [ ] Mock音声入力に対してSTT結果(モック)が処理される
- [ ] Voice Analysisの感情分析・キーワード抽出結果が表示される
- [ ] 話者分離ブリッジ(`DiarizationBridge`、Issue #182)はユニット/結合テストで
  検証済み。実通話音声(チャネル分離済み)での実地確認は次フェーズ
- [ ] **VOICEVOX / whisper.cpp の実エンジンとの接続は次フェーズ Issue
  [#183](https://github.com/grant-inc0801/Musasabi-OS/issues/183) で検証する**

## 8. インストーラー確認

- [ ] `npm run build:desktop`(= `tauri build`)で NSIS `.exe` / MSI `.msi` がビルドできる
  (出力先: `apps/desktop/src-tauri/target/release/bundle/{nsis,msi}/`)
- [ ] GitHub Actions **Beta Build** の手動実行で `musasabi-beta-windows-<sha>`
  artifact(`.exe` / `.msi`)がダウンロードできる
- [ ] インストーラーからのインストール → 起動 → アンインストールが正常に行える
- [ ] タスクバー・ウィンドウ・インストーラに白黒ムササビの仮アイコンが表示される
  (`apps/desktop/src-tauri/icons/`。正式アイコンは後続フェーズ)

## 9. β版評価ビルドの画面操作確認(D-20260706-002)

β版はすべて Mock 構成で動作する。以下を `npm run dev:desktop`(または
`npm run dev:web`)で起動して確認する。

- [ ] 起動直後に初回セットアップウィザードが表示され、完了後に本体UIへ進める
- [ ] サイドバーの部門タブ(営業部 / 出版部 / 開発部 / サポート部)と全社タブ
  (AI社員管理 / Sales Brain / 設定)すべてに遷移できる
- [ ] **営業部(既定タブ)**: 全体KPI・AI社員別KPI(コール結果)の横棒グラフ、
  日次計画・推奨アクション・リード一覧が表示される(売上は表示されない)
- [ ] **AI社員管理**: Company Genome・組織図ツリー・AI社員名簿(7名)が表示され、
  組織単位クリックで名簿が絞り込まれる。「コールトレーニングへ移動」で画面遷移できる
- [ ] **コールトレーニング**: Learning / Test / AutoCall の3モードが見える
- [ ] **Test Mode**: 連絡先(ダミー値)を入力して「テストコール開始」→ AI応答表示 →
  顧客役入力への切り返し → 指摘追加 → 通話終了 まで操作できる
- [ ] **AutoCall Mode**: 「準備中・承認待ち」表示で、開始ボタンが無効(本番実行不可)
- [ ] **Sales Brain**: 学習データソース(Mock/準備中)と共通ナレッジのカテゴリ絞り込みが動く
- [ ] **設定**: AI社員・音声(Mock)・既定コールモードを保存でき、外部サービス
  接続準備状況にダミー値を入力しても実接続が発生しない
- [ ] MUSAアバターが3Dムササビで表示される(詳細は §12)
- [ ] 一連の操作で実API接続・実架電・実認証情報保存が発生しない(Mock表示のみ)

## 10. 右下常駐アバター・ミニパネル(D-20260706-004)

- [ ] 管理画面がダークテーマ+サイドバー構成で表示される(AI社員ステータス・
  モード状態カードがサイドバーに出る)
- [ ] メインウィンドウを **最小化** すると、管理画面が隠れて右下アバターのみ残る
- [ ] メインウィンドウを **X で閉じて** も、アプリは終了せず右下アバターのみ残る
- [ ] アバターがデスクトップ **右下**(タスクバーの上)に表示される
- [ ] **アバターをクリック** するとミニパネルが開き、再クリックで閉じる
- [ ] ミニパネルに現在モードが表示され、Learning / Test / AutoCall を切替できる
- [ ] オートコールモードへ切替すると「承認待ち(本番実行不可)」の注意が出る
  (本番架電は一切実行されない)
- [ ] チャット欄に指示を入力して送信すると、MUSAの応答(Mock)が表示される
- [ ] 「メイン管理画面を開く」でメインウィンドウが復帰する
- [ ] モード切替時に吹き出しに提案(例:「次はTest Modeでロールプレイ確認しましょう」)が
  表示され、クリックで閉じられる
- [ ] システムトレイの「開く」でもメインウィンドウが復帰する

## 11. β版UI追加修正(D-20260706-006)

- [ ] 常駐時(パネル・吹き出しが閉じている間)、**アバター以外の透明な領域が残らない**
  (アバター直上以外をクリックしても背後のアプリに届く)
- [ ] ミニパネル・入力欄・モード切替は **アバターをクリックした時のみ** 表示される
- [ ] ミニパネルのスライダーでアバターサイズを変更できる
- [ ] スライダー操作中・操作後にミニパネルのウィンドウが移動しない(画面外へ流れない)
- [ ] モード切替が縦配置で、選択中モードに緑ランプが点灯する
- [ ] アバターに背景バッジ(円形の枠)がなく、キャラクターのみが表示される
- [ ] 「⠿ 移動」ハンドルのドラッグでアバターの常駐位置を変更できる
- [ ] サイズ変更後もミニパネルのレイアウトが崩れない
- [ ] アプリ再起動後もアバターサイズ設定が保持され、常駐表示にも反映される
- [ ] 左サイドバーに部門名のみが表示され、営業部配下のサブ項目
  (KPI / コールトレーニング / Sales Brain)から各ページへ遷移できる
- [ ] 営業部 > KPI にグラフと表(件数・アポ率・成約率)が表示される
- [ ] AI社員管理の組織図が接続線つきツリー+所属人数バッジで表示され、
  クリックで名簿が絞り込まれる
- [ ] アプリアイコン・サイドバーのブランドマークが白ムササビ/黒角丸で統一されている

## 12. 3Dアバター(Issue #200)

- [ ] 右下アバターが3Dムササビ(浮遊・ゆらぎアニメーション)で表示される
- [ ] スライダーでサイズを変えても3D表示が追従する
- [ ] ミニパネルの「VRMアバターを読み込む(VRoid)」で VRoid Studio 製 .vrm を
  選択すると、3Dムササビと差し替わって表示される
- [ ] VRM表示中にまばたき・呼吸(上下)の待機モーションが動く
- [ ] 業務ステータスの変化(デモ通話分析など)で表情が変わる
- [ ] VRMの読み込みに失敗した場合、吹き出しにエラーが表示されアプリは落ちない
- [ ] 営業部ページがコールシステム中心(部門サマリー+ Learning / Test / AutoCall)になっている
- [ ] 出版部ページに成果物一覧・販売数・売上(Mock)が表示される
- [ ] Learning Mode で日々の作業内容を手動登録でき、再起動後も一覧に残る
- [ ] 上記の操作で実架電・実API接続・外部送信が発生しない(すべてMock/ローカル保存)

## 13. Memory Engine・AutoCall安全ゲート管理

- [ ] 全社ナビの「Company Brain」に行動記録の件数タイル(短期/長期/業務/ユーザー/
  会社/プロジェクト)と履歴一覧が表示される
- [ ] テストコールの開始・終了、指摘の登録、設定保存、プラグイン切替が
  Company Brain の履歴に自動記録される
- [ ] 分類・テキスト検索で履歴を絞り込める
- [ ] アプリ再起動後も Memory の記録が残る(短期メモリは24時間で自動削除)
- [ ] オートコールモードの安全ゲート一覧をチェックボックスで充足/解除でき、
  変更が Company Brain に監査記録される
- [ ] 「実アカウント連携」ゲートはロック表示(🔒)で充足できず、
  「オートコール開始」ボタンが有効化されない
- [ ] ゲート状態がアプリ再起動後も保持される
- [ ] 上記の操作で実架電・実API接続・外部送信が発生しない(すべてローカル保存)

## 14. 議事録の自動生成(Development Bible 第10章)

- [ ] テストコールを終了すると、会話欄の下に「議事録(自動生成)」が表示される
- [ ] 議事録に要約(感情・キーワード・発話比率)、参加者、決定事項、
  宿題・フォローアップが表示される
- [ ] 「申し込みます」等の発話が決定事項に、「後日お送りします」等が宿題に抽出される
- [ ] 議事録作成が Company Brain の履歴(業務)に記録される
- [ ] 上記の操作でLLM推論・外部送信が発生しない(決定的ロジック・ローカルのみ)

## 15. Vision — 手動オプトイン画面解析(Development Bible 第11章)

- [ ] 全社ナビの「Vision」で「この画面を解析する」を押すと、自アプリ画面の
  UI認識結果(ボタン/入力欄/見出し/ウィンドウの件数タイル)が表示される
- [ ] 操作可能なボタンの一覧(ラベル・位置・サイズ)が表示され、disabled の
  ボタンは含まれない
- [ ] 抽出テキスト・ウィンドウ情報が表示される
- [ ] 解析の実行が Company Brain の履歴(業務)に記録される
- [ ] ボタンを押さない限り解析は一切実行されない(常時監視なし)
- [ ] 他アプリ・デスクトップ全体は解析されない(自アプリのみ。実画面キャプチャ・
  実OCRは未実装)
- [ ] 上記の操作で外部送信が発生しない(ローカル処理のみ)

## 16. Automation — 手動オプトイン操作記録→再実行(Development Bible 第12章)

- [ ] 全社ナビの「Automation」で「記録を開始」を押した間のみ、アプリ内の
  ページ遷移が記録される(開始前・停止後の操作は記録されない)
- [ ] 「記録を停止して保存」でルーチン(名前・操作列)が保存され、一覧に表示される
- [ ] 「再実行」で記録した順に画面が自動で遷移し、再実行回数が増える
- [ ] アプリ再起動後もルーチンが残る
- [ ] 記録開始・保存・再実行が Company Brain の履歴(業務)に監査記録される
- [ ] 他アプリ・OS操作は記録されない(アプリ内ページ遷移のみ)
- [ ] 上記の操作で外部送信が発生しない(ローカル保存のみ)

## 17. Self Improvement — 短期→長期メモリ昇格

- [ ] Company Brain の「自己改善」で昇格ボタンを押すと、同じ行動が2回以上
  繰り返された短期メモリが長期ナレッジ1件に集約されて昇格する
- [ ] 昇格した長期メモリ(「学習事項に昇格: …」)が履歴に表示される
- [ ] もう一度押しても同じ内容は二重昇格されない
- [ ] 昇格対象がない場合はその旨が表示される
- [ ] 上記の操作でLLM推論・外部送信が発生しない(決定的ロジック・ローカルのみ)

## 18. 架電リスト制作課(開発部)

- [ ] サイドバーの 開発部 > 架電リスト制作課 でページが開く
- [ ] 都道府県プルダウン(47件)と市区町村の自由入力欄が表示され、
  「+ 入力欄を追加」で複数の市区町村を入力できる(削除も可能)
- [ ] 「検索」で店舗一覧(店舗名・郵便番号・住所・電話番号・ジャンル・営業時間・
  デリバリー有無・媒体)が表示される(APIキー未入力時はMockデータ)
- [ ] SerpAPIキーを入力して検索すると、Googleマップの実データが取得され
  「データソース: SerpAPI(Googleマップ実データ)」と表示される
- [ ] APIキーがlocalStorage等に保存されない(アプリ再起動で消える)
- [ ] 実データではデリバリー媒体列が空欄になる旨の注記が表示される
- [ ] 無効なキーの場合、日本語のエラーメッセージが表示されアプリは落ちない
- [ ] 件数集計(合計・デリバリー対応・市区町村別・ジャンル別)が表示される
- [ ] 「Excel出力(.xlsx)」で `架電リスト_<都道府県><市区町村>.xlsx` がダウンロード
  され、Excel で正しく開ける(8列+データ行)
- [ ] Windows実機でダウンロードファイル名に日本語が保持される
  (headless検証では確認不可のため実機確認)
- [ ] 検索・Excel出力が Company Brain の履歴(業務)に記録される
- [ ] 上記の操作で実API接続・外部送信が発生しない(Mock・ローカル処理のみ)

## 19. UIフィードバック第3弾(アコーディオン・輪郭フィット・メタリック調・アイコン)

- [ ] サイドバーの部署ボタン(営業部/開発部)押下で小分類項目が開閉する
  (▸/▾ 表示。サブ項目の無い部署はそのままページ遷移)
- [ ] 右下常駐アバターの周囲に膜・影が見えず、キャラクターの輪郭外は完全透過
- [ ] ミニパネルはパネルの角丸輪郭内のみ表示され、周囲に膜・影が出ない
- [ ] 管理画面がシルバーグレーのメタリック調(明るい金属質感の地+白系ガラスパネル)
  で表示され、文字・表・グラフの視認性が保たれている
- [ ] アプリアイコン(タスクバー/ショートカット)のムササビ輪郭が滑らかで、
  32pxでも判別できる

## 20. UIフィードバック第4弾(パネル実寸フィット・アバター作成・媒体検索・保存先選択)

- [ ] ミニパネルを開いたとき、ガラス面がパネル+アバター領域の実寸に収まり、
  パネルより大きな膜が出ない(チャットが増えるとパネルに合わせて伸びる)
- [ ] ミニパネルに「VRMアバターを読み込む」ボタンが無い(設定へ移行済み)
- [ ] 設定 > アバター作成で、3Dプレビュー・カラー調整(体/腹/目)・
  感情モーション6種(通常/喜び/怒り/悲しみ/リラックス/驚き)を確認できる
- [ ] アバター作成でVRMを取り込みプレビューできる
- [ ] 「保存して反映」を押すと右下の常駐アバターへカラー/VRMが反映される
  (アプリ再起動後も保持)
- [ ] 架電リスト: 市区町村が空欄のまま検索すると都道府県全域で検索され、
  集計に「全域」タイルが表示される
- [ ] 架電リスト: 「媒体検索」で各店舗の利用デリバリー媒体が照合され、
  同じリストの媒体列に統合される(SerpAPIキー入力時はGoogle検索実データ)
- [ ] Excel出力を押すと保存先を選択するダイアログが開き、選んだ場所に保存される
  (キャンセルも可能)

## 21. ミニパネル2階層メニュー(コールシステム/業務支援)

- [ ] ミニパネルに大項目「コールシステム」「業務支援」が表示され、
  押下で小項目が開閉する(▸/▾)
- [ ] コールシステムの小項目(Learning/Test/AutoCall)の選択中に緑ランプが点灯する
- [ ] 業務支援 > Learning で作業内容を入力して学習させられる
  (メイン画面のラーニングモード一覧にも反映)
- [ ] 業務支援 > Development で「記録を開始」→ メイン画面でのページ移動が記録され、
  記録中はDevelopmentのランプが点灯する
- [ ] 名前を付けて「記録を停止して保存」すると自動化として保存される
- [ ] 業務支援 > Support で保存した自動化の名前一覧が表示され、選択すると
  選択項目にランプが点灯し、メイン画面で自動化が実行される
- [ ] 実行完了時に吹き出しで通知される

## 22. 営業リスト(第13章)

- [ ] 架電リスト制作課の「営業リストへ取り込む」で抽出店舗が営業リストへ追加され、
  再取込時は重複(電話番号一致)が追加されない
- [ ] 営業部 > 営業リストにステータス別の集計タイル(未架電/架電済/アポ獲得/
  成約/対象外)と一覧が表示される
- [ ] 一覧でステータス・メモを変更でき、アプリ再起動後も保持される
- [ ] ステータスで絞り込みできる
- [ ] 「テストコールへ」で連絡先が引き継がれ、コールトレーニングの
  テストモードが開く(実架電はしない)
- [ ] ステータス更新・取り込みが Company Brain の履歴に記録される

## 23. Musasabi Command Center(D-20260706-007)

- [ ] 起動直後の画面が部署パネル中心の Command Center になっている
- [ ] 左サイドバーはアプリアイコン・ロゴ・全社員数(稼働中)・稼働率ゲージ・
  設定ボタンのみ
- [ ] 8部署のパネルに部署名・社員数・ステータスのみが表示され、
  ステータス色(完了=緑/作業中=黄/エラー=赤/承認待ち=紫)でランプと枠が発光する
- [ ] ホバーで枠の光が強くなる
- [ ] 連携部署間(営業→CS、CS→マーケ、出版→企画、企画→経理、開発→人事)が
  白い発光ライン+ノード点で結ばれ、流れるアニメーションが動く
- [ ] パネルクリックで右側に詳細(ステータス/作業内容/進捗/ログ)が表示される
- [ ] 営業部詳細にコールシステムMock(架電数/接続率/アポ/成約/コール状況)が出る
- [ ] システム開発部詳細にエラー原因と対処が表示される
- [ ] 下部チャットで指示先部署をプルダウン選択し、テンプレート・添付・
  音声入力Mock・送信が動作する
- [ ] 右下にマスコットアバターが表示され、吹き出しで承認待ち・エラー原因・
  解決策・全体進行が要約される
- [ ] 「詳細を見る」「設定」から従来画面へ遷移でき、
  「⌂ コマンドセンター」で戻れる

## 24. Command Center 実データ連動・営業リストExcel出力

- [ ] データが無い状態では営業部詳細のコールシステムが「Mock」と明示されて表示される
- [ ] テストコール実施・営業リストのステータス変更後、Command Center の営業部詳細が
  「実データ」表示になる(架電数=テストコール累計/完了率/アポ獲得数/成約数/
  未架電リード数)
- [ ] 営業部パネルのステータスが実データから導出される(未架電リードあり=作業中、
  なし=完了)
- [ ] 営業部詳細の作業ログに Memory の業務記録(最新5件)が表示される
- [ ] 営業リストページの「Excel出力(.xlsx)」で現在の絞り込みを反映した一覧
  (8列: 店舗名〜更新日時)が保存でき、Excelで開ける

## 25. データ管理(バックアップ/復元)

- [ ] 設定 > データ管理で「バックアップをエクスポート」を押すと保存先を選択して
  JSONファイルが保存される
- [ ] 「バックアップから復元」でファイルを選ぶと確認の後に復元され、
  再読み込み後にデータ(Memory・営業リスト等)が戻っている
- [ ] 「すべてのローカルデータを削除」は確認の後に musasabi.* データを削除する
- [ ] 不正なファイルを選んだ場合は日本語エラーになり、データは変更されない
- [ ] VRMファイルはバックアップに含まれない旨が表示されている

## 26. 市場調査部・出版部クリーン運営(D-20260706-009)

- [ ] 部署一覧に「市場調査部」(5人)が表示され、連携ライン
  (→開発部/企画部/出版部)が結ばれている
- [ ] 市場調査部の詳細パネルに KPI(本日収集/新サービス/技術評価/開発部連携/
  企画部連携/CEO提案待ち/採用候補)が表示される
- [ ] AI最新情報・新サービス一覧・技術評価一覧・AI組み合わせ候補・
  部署間連携/CEO提案・採用済み/保留・AI社員5名がMock表示される
- [ ] チャットの指示先部署プルダウンに「市場調査部」がある
- [ ] 出版部の詳細パネルに「クリーン運営/規約チェック」(9項目・ステータス
  ランプつき)と出版部AI社員5名が表示される
- [ ] アバター吹き出しに市場調査部の発見と出版部の承認待ちが要約される
- [ ] 実Web巡回・実API接続・実出版/投稿/販売は発生しない(すべてMock)

## 27. 保管庫(Knowledge Vault)・容量管理・企画部資料フロー(D-20260706-010)

- [ ] 部署一覧に「保管庫」パネル(保管件数/使用容量/容量ステータス)が表示される
- [ ] 保管庫を開くと保管データ一覧(タイトル/種類/部署/AI社員/バージョン/
  更新日/サイズ/タグ/ステータス)が表示される
- [ ] プレビューMock(サムネイル+バージョン履歴)とダウンロードMockが動作する
- [ ] 使用容量/上限容量/使用率と容量ステータス(正常=緑/注意=黄/危険=赤)が表示される
- [ ] 大容量ファイル・重複候補・アーカイブ候補・削除候補の件数が表示される
- [ ] キーワード/種類/部署の絞り込みと更新日順/サイズ順の並べ替えができる
- [ ] アーカイブ/削除候補に追加(Mock)が動作し、保護フラグ付きは削除候補にできない
- [ ] 部署別・種別の容量使用量と保存ルール(肥大化防止)が表示される
- [ ] 企画部詳細に資料作成業務+保管庫フロー(作成中/保存待ち/保存済み/更新必要/
  容量注意)が表示され、「保管庫へ保存(Mock)」が動作する
- [ ] アバター吹き出しに保管庫の保存完了・使用率が要約される
  (詳細パネル表示中は吹き出しが自動的に閉じ、アバタークリックで再表示)
- [ ] 実ファイル操作・実クラウド接続は発生しない(すべてMock)

## 28. コア部署の完成(企画部/市場調査部ページ・出版部クリーン運営統合)

- [ ] サイドバーに「企画部」「市場調査部」ボタンがあり、押下で各ページへ遷移する
- [ ] 企画部ページに資料作成業務・資料状況6区分・「保存待ち資料を保管庫へ保存
  (Mock)」ボタン・保管庫の状態(容量ランプ)・企画部の保管資料一覧が表示される
- [ ] 市場調査部ページに KPIタイル7件・AI最新情報・新サービス+技術評価・
  AI組み合わせ研究・部署間連携/CEO提案・採用状況・AI社員5名が表示される
- [ ] 出版部ページに「クリーン運営/規約チェック」9項目(ステータスランプつき)と
  出版部AI社員5名が表示される
- [ ] コマンドセンターの企画部/市場調査部の詳細パネル「詳細を見る」から
  各従来ページへ遷移できる
- [ ] 企画部の「保管庫へ保存(Mock)」押下で Memory に業務記録が残る
- [ ] 実ファイル操作・実Web巡回・実API接続は発生しない(すべてMock)

## 29. 管理部門ページ(マーケティング部/経理部/人事部)

- [ ] サイドバーに「マーケティング部」「経理部」「人事部」ボタンがあり、
  押下で各ページへ遷移する
- [ ] マーケティング部ページに KPI4タイル・キャンペーン効果測定テーブル・
  SNS投稿計画・AI社員4名が表示される
- [ ] 経理部ページに月次サマリー(売上/経費/収支/未確定)・仕訳・経費一覧・
  AI社員2名が表示される
- [ ] 人事部ページに KPI4タイル・AI社員の稼働/評価テーブル・採用計画・
  AI社員2名が表示される
- [ ] コマンドセンターのマーケティング部/経理部/人事部の詳細パネル
  「詳細を見る」から各従来ページへ遷移できる
- [ ] 実広告出稿・実SNS投稿・実会計データ連携・実採用活動は発生しない
  (すべてMock)

## 30. Command Center チャット強化(部署Mock応答+履歴)

- [ ] チャットで部署を選び「進捗状況を教えて」を送信すると、その部署の
  進捗率+直近ログを含むMock応答が表示される
- [ ] エラー中の部署(システム開発部)に「課題を報告して」を送ると
  原因と対処を含む応答が返る
- [ ] 承認待ちの部署に課題を聞くと承認待ちの旨が返る
- [ ] アプリを再起動してもチャット履歴(直近分)が復元される
- [ ] 部署詳細パネルに「直近の指示(チャット)」としてその部署あての
  指示(最大3件)が表示される
- [ ] チャット履歴はバックアップ/復元(設定 > データ管理)の対象に含まれる
- [ ] 実AI API・外部送信は発生しない(応答はローカルのMock生成)

## 検証結果の記録

検証を実施したら、結果(OK/NG、発生した問題、スクリーンショット等)を
`docs/ai-handoff/STATUS.md` または新しい issue に記録し、
`docs/ai-handoff/CLAUDE_RESPONSE.md` から参照できるようにすること。
