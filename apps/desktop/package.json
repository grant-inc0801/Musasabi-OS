{
  "name": "@musasabi/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix ../../packages/shared run build && npm --prefix ../../packages/avatar-2d run build && npm --prefix ../../packages/ai-core run build && npm --prefix ../../packages/integrations run build && npm --prefix ../../packages/voice-analysis run build && npm --prefix ../../packages/voice-engine run build && npm --prefix ../sales-workspace run build",
    "dev": "npm run build && tauri dev",
    "package:win": "npm run build && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
