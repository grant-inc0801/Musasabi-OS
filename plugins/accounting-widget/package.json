{
  "name": "@musasabi/plugin-accounting-widget",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm --prefix ../../packages/plugin-sdk run build && tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@musasabi/plugin-sdk": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.5.4"
  }
}
