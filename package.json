{
  "name": "musasabi-os",
  "version": "0.1.0",
  "private": true,
  "description": "Musasabi OS monorepo - Epic β-001: Sales Department Operational Release",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@musasabi/shared --workspace=@musasabi/avatar-2d --workspace=@musasabi/ai-core --if-present && npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
